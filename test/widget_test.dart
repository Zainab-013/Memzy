import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Memzy App Smoke Test', (WidgetTester tester) async {
    // Ensure the testing framework initializes cleanly
    expect(true, isTrue);
  });
}
