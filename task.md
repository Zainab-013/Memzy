# Task List - Memzy: Personal Memory Assistant

- `[/]` Initialize Flutter project in the workspace directory.
- `[ ]` Add dependencies to `pubspec.yaml` and fetch packages.
- `[ ]` Configure Native Android configuration for "Forward to Memzy" sharing intent.
- `[ ]` Implement Data Models (`Chat`, `Message`, `Reminder`).
- `[ ]` Set up Storage Service (Hive database & local storage).
- `[ ]` Implement Natural Language Reminder Parser (`ReminderParser`).
- `[ ]` Set up Notification Service (`NotificationService`).
- `[ ]` Set up State Management (`MainProvider`).
- `[ ]` Build App Entry Point (`main.dart` with Theme and Provider initialization).
- `[ ]` Create UI Screens:
    - `[ ]` Splash Screen (`splash_screen.dart`)
    - `[ ]` Home Screen (`home_screen.dart` - Chats Tab with Search & Theme Toggle)
    - `[ ]` Chat Screen (`chat_screen.dart` - Conversation view, bubbles, input area)
    - `[ ]` Chat Info Screen (`chat_info_screen.dart` - Media, Links, Docs, and Reminders)
    - `[ ]` Reminders Screen (`reminders_screen.dart` - Today, Upcoming, Completed list)
    - `[ ]` Share Target Selection Dialog (`share_target_dialog.dart`)
- `[ ]` Verify application compilation and perform runtime testing.
